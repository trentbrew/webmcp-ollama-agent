## 2026-09-01 — Impl + verify

- Model picker moved after paperclip in `ChatComposer.svelte` action row.
- Discovered tools pill: `is-discovered` class, green tokens, `chat-composer-discovered-pulse` keyframes.
- Trellis issue TRL-19 created with check/unit suites + grep ACs for toolbar order and pulse animation.
